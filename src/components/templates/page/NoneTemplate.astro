---
import { Image } from "astro:assets";
import { type CollectionEntry, render } from "astro:content";

interface Props {
    page: CollectionEntry<"page">;
}
const { page } = Astro.props;
const { title, featured } = page.data;
const { Content } = await render(page);
---

<Content />
